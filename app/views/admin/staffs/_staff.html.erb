<tr id="<%= dom_id(staff) %>">
  <td><%= staff.email %></td>
  <td><%= staff.first_name %></td>
  <td><%= staff.last_name %></td>
  <td><%= staff.phone_number %></td>
  <td><%= date_format(staff.created_at, "datetime") %></td>
  <td><%= date_format(staff.updated_at, "datetime") %></td>
  <td class="text-end">
    <div class="d-flex gap-3">
      <%= link_to raw("<i class='mdi mdi-eye font-size-18'></i>"), send("#{current_admin_panel.class_name}_staff_path", staff), class: "text-primary", "data-bs-toggle": "tooltip", "data-bs-placement": "top", "title": "View detail" if action_policy("show") %>
      <%= link_to raw("<i class='mdi mdi-pencil font-size-18'></i>"), send("edit_#{current_admin_panel.class_name}_staff_path", staff), class: "text-success", "data-bs-toggle": "tooltip", "data-bs-placement": "top", "title": "Update" if action_policy("update") %>
      <%= link_to raw("<i class='mdi mdi-delete font-size-18'></i>"), send("#{current_admin_panel.class_name}_staff_path", staff), class: "text-danger", data: { turbo_method: :delete, turbo_confirm: 'Are you sure?' }, "data-bs-toggle": "tooltip", "data-bs-placement": "top", "title": "Delete" if action_policy("destroy") %>
    </div>
  </td>
</tr>
